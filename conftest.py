# Placing conftest.py at the project root puts this directory on sys.path,
# so tests in tests/ can import top-level modules like logic_utils when run
# with a plain `pytest` invocation (not just `python -m pytest`).
